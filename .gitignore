# --- DEPENDENCIES (WAJIB IGNORE) ---
node_modules/
package-lock.json   # Opsional: Beberapa orang suka commit ini, beberapa tidak. Kalau ragu, commit saja (hapus baris ini).
yarn.lock

# --- DATABASE (SANGAT WAJIB IGNORE) ---
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
/electron/database/*.db

# --- BUILD RESULT (WAJIB IGNORE) ---
dist/
dist-electron/
release/
build/

# --- LOGS ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- OS FILES ---
.DS_Store
Thumbs.db

# --- ENVIRONMENT VARIABLES ---
.env
.env.local